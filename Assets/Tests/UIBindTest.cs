using RPG.Core.ExtensionMethods;
using InventorySystem;
using UnityEngine;
using UnityEngine.UIElements;

public class UIBindTest : MonoBehaviour
{
    //Inventory inv;
    //Image icon;
    //UI_BaseInventorySlot slot;
    //public StyleSheet style;

    //void Start()
    //{
    //    GetComponent<UIDocument>().rootVisualElement.styleSheets.Add(style);

    //    var source = GetComponent<BindTest>();
    //    GetComponent<UIDocument>().rootVisualElement.Q<TextElement>("TextField").BindProperty(source.myint);

    //    inv = GetComponent<BaseCharacter>().bag;
    //    icon = new Image();
    //    GetComponent<UIDocument>().rootVisualElement.Q<VisualElement>("ImageContainer").Add(icon);
    //    icon.BindProperty(source.mySprite);

    //    slot = GetComponent<UIDocument>().rootVisualElement.Q<UI_BaseInventorySlot>("slot");
    //    slot.BindProperty(source.myItemSlot);
    //}
}
