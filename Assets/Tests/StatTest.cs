using UnityEngine;
using RPG.Core.Character.Attributes;

public class StatTest : MonoBehaviour
{
    //public StatBlock stats;

    private void Start()
    {
        //stats = GetComponent<StatBlock>();

        //if (stats.GetAttribute<RPGVital>("health", out RPGVital vital))
        //{
        //    Debug.Log(vital.CurrentValue);
        //}
    }
}